import React from "react";
import LoginSignupForm from "../components/login/LoginSignupForm"; // Correct path to the component

const LoginPage = () => {
    return (
        <div>
            <LoginSignupForm />
        </div>
    );
};

export default LoginPage;
